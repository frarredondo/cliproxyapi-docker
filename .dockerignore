.env
.env.example
.git
.gitignore
config/
auths/
logs/
docker-compose.yml
README.md

# Host-side version tooling; never part of the build context.
Makefile
bin/
cliproxy.lock

# Neither image COPYs from the local context, so the Dockerfiles themselves
# can stay out of it too.
Dockerfile
Dockerfile.keeper
